extends ../layouts/main-layout.pug

block styles
    link(rel="stylesheet" href="/css/forms.css")

block content
    form(action="/admin/add-bayi" method="POST")
        input(type="hidden" name="_csrf" value=`${csrfToken}`)
        .form-group.row
            label(for="name").col-sm-2.col-form-label Bayi adı
            .col-sm-10
                input.form-control#name(type="text" name="name")  

        .form-group.row
            label(for="no").col-sm-2.col-form-label Bayi no
            .col-sm-10
                input.form-control#no(type="text" name="no") 
                
        //- .form-group.row
        //-     label(for="categoryid").col-sm-2.col-form-label Category
        //-     .col-sm-10
        //-         select.form-control#categoryid(name="categoryid")
        //-             option(value="-1") Select Category
        //-             each category in categories
        //-                 option(value=`${category.id}`) #{category.name}

        .form-group.row
            label(for="description").col-sm-2.col-form-label Adres
            .col-sm-10
                textarea.form-control(name="description" cols="30" rows="10") 
                
        .form-group.row
            .col-sm-10.offset-sm-2
                input(type="submit" value="Bayi Kaydet").btn.btn-primary   
           
